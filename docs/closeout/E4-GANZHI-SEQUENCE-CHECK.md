# E4 · 干支序列连续性检查（文本层，不读图）：工具、判据与本次结果

日期：2026-09-12｜工作树 `/Users/yuhanglin/fateradar-goal-20260912/classics`

## 1. 这不是什么，是什么

**不是**新的转写方法，**也不是**读图替代品。它是一层**筛查**：转写稿里的干支串（行标列、六十甲子环、逐日/逐时表）
若在六十甲子环上**步长不恒定**，就值得按图复核——因为读错或漏抄一个字就会打断等差。

价值在于：**不需要读图能力也能跑**，且它是**独立的**（只看代码里那份转写文本，不看转写者的自述）。
本次它在一个文件上**独立复现了转写者自己标注的疑点**（见 §3），这是对它的交叉验证，也是对那份转写的旁证。

## 2. 判据

- 只检查**长度 ≥ 3** 的连续干支串（两字相邻在散文里极常见，不作为序列）。
- **合规 = 步长恒定**：+1（六十甲子）、+15（十五日／三元分节）、+10（旬首）等皆可，升序降序皆可。
- **不合规 = 步长不恒定**（如 `甲子 乙丑 丙寅 丁卯 戊辰 丁卯`：前五步 +1，最后一步 −1）。

命令：`python3 tools/check-ganzhi-sequences.py [路径...]`（缺省扫 `sources/normalized/{san-shi,bazi}/**/*.md`，有命中则退出码 1）。

## 3. 本次结果（22 文件｜46 条干支串｜步长不恒定 20 条）

20 条命中里，绝大多数是**散文里的枚举**，例如：

- `ocr-batch-0021-0044.md L81/82/94`「甲子甲午己卯己酉」：原文句为「凡節氣或遇甲子甲午己卯己酉是甲星當用第幾局」，
  是**纸上枚举顺序**，不是历日顺序（同文件 L67 另有一处 `甲子 己卯 甲午 己酉`，那是 +15 分节，合规）。
- `nlc-recovery.md` 的 11 处：散文。

**1 处是真信号，且与被查文件自己的疑点吻合**：

- `ocr-batch-0280-0298.md L98`：`甲子・乙丑・丙寅・丁卯・戊辰・丁卯` —— 工具在**没有读该文件 doubt 块**的情况下标出这一行；
  而该文件的 `doubt_281` 已写：「末列作「丁卯」与第 4 列重复，而按六旬常规此处应另有一柱。**我按纸面对位登记所见，不据常规改正**」。
  两者独立指向同一处 → 该处**需要一次原图复核**，见 §4。

## 4. 待办（已派单）

`ocr-batch-0280-0298.md` p281 的 C6–C11 六列「时支行头」：末列到底是「丁卯」（与第 4 列重复）还是另一柱？
必须**回原图逐列复核**，不得据六旬常规改正（转写者与工具的判断都不能代替纸面）。
**工具不能判定哪一个是错的**，它只能指出「这里不连续」。

## 5. 边界

- 命中 ≠ 错误：本工具只算步长，不理解句意；20 条命中里 19 条已解释为散文/枚举。
- 未读图的结论一律不升级任何注解状态；本工具不产生、不修改转写文本。
- 工具只覆盖**干支串**这一种可文本化的自检；字形、版式、行标归属仍须读图。
